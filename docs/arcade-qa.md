# アーケード検証記録

2026-09-08更新。WebGL／Android実機の検証が残るため、完成・公開済みとは判定しない。

## 自動検査

- JavaScript構文検査、入口2ファイルの一致：合格。
- 物理オブジェクトの10リンク：ソースと配信物の両方で実在を確認。
- 配信物のHTML内179件のローカル参照：欠落なし。
- 実際のThree.jsジオメトリ／world matrix／raycast／プレイヤー半径を使用した空間検査：合格。
- 中央通路の端から端、後端と入口の境界、空き店のシャッターに衝突することを確認。
- 到達可能な床6,977地点を検査。妹字屋452、しきがみ上手242、祭祀具413、未祓い493、作品保管庫605の店内地点へ到達可能。
- 十一個の反応物すべてが、歩いて到達できる場所から見え、操作距離に入ることを確認。
- 既存 `public/` のトップ・商品ツリー・理論・データに差分なし。

## ブラウザでの操作

- 通常の公開用 `/arcade/` を起動：検証用Chromeの `GL_VENDOR = Disabled` によりWebGL contextを生成できず、エラーと再読込ボタンを表示した。
- 操作検証器は製品の `main.js` と `world.js` をそのまま使用し、描画器だけCPU版へ交換した。実際のブラウザ操作でWASDによる街路移動・妹字屋への入店・矢印とマウスドラッグによる見回しを確認。
- 1280×800で入口から妹字屋の机まで歩き、Eキーで暗号台帳を開いて、既存の妹字暗号ラボと既定の字形コードが表示された。
- 縦390×780のブラウザ内画面で方向ボタンを選び、しきがみ上手へ入店・退出。WASDで妹字屋・祭祀具工房へ入店・退出。いずれも座標診断を確認した。
- 縦画面で右側ドラッグによる見回し、台帳の対象表示ボタン、ラボへの遷移を確認。ブラウザの戻る→「歩く」で位置(-3.53, 15.13)・方位-70°・仰角-29°が復元された。
- 修正版1280×800で入口から歩き、未祓い観測(3.94, 5.49)と作品保管庫(-4.40, -1.23)へ入店・退出。そのまま通りの奥(-0.26, -13.11)に到達した。全5店舗の入店をブラウザ入力で確認。
- 縦画面のbody/clientWidthとscrollWidthはいずれも390、clientHeightとscrollHeightはいずれも780。横スクロールなし。
- 横844×390でもbodyの幅とscrollWidthは844、高さとscrollHeightは390。操作パネルの開閉、軽量設定、動きを止める設定、入口への復帰、音ボタンの状態切替を確認。音の聴感は未評価。
- CPU描画器は大きな床・屋根の三角形の前後関係を正確に表現できず、入口で背景に覆われる場合がある。これは操作検証器の制約として残し、美術の合否は判定していない。
- 既存の全66HTML静的QAとDeepDream v21の既存回帰検査が合格。

## 今回修正して再検査した点

- FPSをフレーム時間の中央値から経過時間の平均へ変更。間欠的な長いフレームを隠さず、自動DPR調整に反映。
- WebGL context loss中は移動入力を無効化し、復帰時に残った入力を破棄。
- WebGL起動失敗の案内と接続失敗の案内を分離。
- 配信する札所保存版のmeta descriptionを補い、検証器の通常リンクを実在ページへ修正。
- QAの文章検査から同梱vendorとnode_modulesを除外。自作コードと公開HTMLの検査は継続。

## 公開判定に残る検査

1. WebGL2を使用可能なPC Chromeで実際の陰影・透過・テクスチャ・初回ロードとpointer lockを確認。
2. Android Chrome実機で、縦横切替、2本の指による同時移動／視点操作、キャンセル、戻る操作を確認。
3. Android実機の標準設定で街路と各店内を歩き、FPS／フレーム時間と描画数を記録。`?qa=1` は診断表示、`?touch=1` はタッチUIの強制表示。CPU検証器のFPSを代用しない。
4. 実機で美術・快適性まで確認してから、WIP表示のないこの候補を完成版として判定する。

この環境では実機の検証を行ったと主張できない。自動検査や静止画のみで完成扱いにしない。
