# ritual-pv

祭祀技師PV向けの将来拡張テンプレート。

現時点では既存 `edit-plan.json` 経路を使う。
次段階で `film-plan.json` とshot型（vrm / video / still / typography / diagram / black / generated-video）を追加する。

このディレクトリは映像文法のプリセット置き場として使用する。
